/**
 * WhatsApp Infrastructure
 * Exports WhatsApp adapter and related infrastructure components
 */

const WhatsAppAdapter = require('./adapter');

module.exports = {
    adapter: WhatsAppAdapter
};
