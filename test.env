# Test Environment Variables
# This file is used for testing purposes only

# Test database (in-memory or temporary file)
TEST_DB_PATH=:memory:

# Test storage directories (use temp directories)
STORAGE_DIR=./storage/test
TEMP_DIR=./storage/test/temp

# Mock API keys (not real keys)
SPOTIFY_CLIENT_ID=test_client_id
SPOTIFY_CLIENT_SECRET=test_client_secret
YOUTUBE_API_KEY=test_youtube_key

# Test server configuration
SERVER_HOST=localhost
SERVER_PORT=3001

# Disable auto-open browser in tests
NODE_ENV=test

